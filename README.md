# autopush
A simple program for an automatic push to the github when you only need to create and push a single commit for all the files in a working directory.
To use this, just copy the file auto.py in your git directory and in a command line write:

    python auto.py <COMMIT MESSAGE>
